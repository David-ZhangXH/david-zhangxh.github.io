# Village 2.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Checkbox steps.

**Goal:** David's village redesign: a one-screen town, click-to-move + WASD, four *enterable* interiors furnished with his real objects, richer Stardew-leaning art, his arcade wording, the coffee machine retired.

**Scope, per David:**
- Town map small enough to see everything on entry (desktop letterboxed full-map view; phones keep camera-follow).
- Move with WASD *and* mouse: click a door/anywhere → auto-walk (BFS) → doors auto-enter.
- Interiors are walkable rooms, not paper lists. HOME: music box, TV, gaming laptop, music corner (guitar/bass/piano/mic), bookshelves, picture board, toy, big life-trajectory book, travel world map (stops+when, pictures later), video tape, and the **secret memory placard** gated by "What is my nickname?" (flower / huahua / 花花). SCHOOL: three memory stations — Yuying, 101, BU. LAB: three tables — Biology, Chemistry, Environmental Science (his minors). LIBRARY: document shelves (retrievable life documents) + sidebar quiz: favorite band → Linkin Park; bilibili account → Insomania Radio; cat's name → huahua (feifei among options); 3/3 → prize (picture TBD by David; placeholder prize panel).
- Arcade start screen: plain rules + "Middle School record — 26.00s" + "beat my record and win a secret prize." No flavor lines.
- Quests: coffee → **memory** (unlock the placard). Stale stored ids filtered.

## Tasks
- [x] 1 · content v2 (`village.json` restructure: home objects/schools/lab/library docs+quiz/npcs) + `validateVillage` v2 (TDD)
- [x] 2 · quests v2 (`memory` replaces `coffee`; stored-id filter) (TDD)
- [x] 3 · scenes model (`map.js`: town 26×16 + four interiors, per-scene walkability/zones/exit tiles, BFS-reachable zones) (TDD)
- [x] 4 · sprites v2 + renderer v2 (fit-whole-map letterbox, outlines/shading, furnished interiors) — screenshot loop
- [x] 5 · index v2: scene switching, click-to-move pathing, object panels, memory gate, library quiz+prize, arcade text, NPCs, quest wiring
- [x] 6 · e2e-village rewrite; full matrix; merge; zip v4

Self-check: every David-listed object present in home; three schools; three lab tables; quiz answers exact (linkin park / insomania radio / huahua); nickname answers case-insensitive incl. 花花; no coffee remnants; record text verbatim.
